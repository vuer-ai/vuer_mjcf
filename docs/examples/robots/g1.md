# G1 Humanoid

The G1 is a humanoid robot with full-body articulation.

## Usage

```python
from vuer_mjcf.robots.g1 import G1
from vuer_mjcf.stage_sets.g1_scene import G1Scene

# Create G1 robot
g1 = G1(name="g1", pos=[0, 0, 0])

# Or use pre-configured scene
scene = G1Scene()
```
